"""Test package for rag-eval-harness.

Run the whole suite with ``make test`` (or ``python -m pytest``). Every module
is importable without any optional dependency installed - the embedding and
LLM-judge adapters are exercised through injected fakes.
"""
